// COLLABORATORS MARQUEE - PENDING PERMISSIONS
// Once you have permissions from all collaborators, rename this file to CollaboratorsMarquee.tsx
// and import it into Index.tsx between Careers and Connect sections.
//
// Required CSS is already in index.css (search for "marquee")

import React from 'react';

const CollaboratorsMarquee = () => {
  return (
    <section className="relative py-6 md:py-8 overflow-hidden border-y border-[hsl(var(--border))]">
      <span className="text-xs uppercase tracking-widest text-[hsl(var(--text-secondary))] opacity-60 block text-center mb-4">Collaborators</span>
      <div className="marquee-container">
        <div className="marquee-track animate-marquee-left">
          {[...Array(4)].map((_, i) => (
            <div key={i} className="flex items-center gap-12 md:gap-16">
              <span className="text-base md:text-lg font-medium text-[hsl(var(--text-secondary))] whitespace-nowrap opacity-50 hover:opacity-100 transition-opacity">Hermes Medical Solutions</span>
              <span className="text-base md:text-lg font-medium text-[hsl(var(--text-secondary))] whitespace-nowrap opacity-50 hover:opacity-100 transition-opacity">DeepC</span>
              <span className="text-base md:text-lg font-medium text-[hsl(var(--text-secondary))] whitespace-nowrap opacity-50 hover:opacity-100 transition-opacity">Zenta</span>
              <span className="text-base md:text-lg font-medium text-[hsl(var(--text-secondary))] whitespace-nowrap opacity-50 hover:opacity-100 transition-opacity">dottxt</span>
              <span className="text-base md:text-lg font-medium text-[hsl(var(--text-secondary))] whitespace-nowrap opacity-50 hover:opacity-100 transition-opacity">Radixlab Medical</span>
              <span className="text-base md:text-lg font-medium text-[hsl(var(--text-secondary))] whitespace-nowrap opacity-50 hover:opacity-100 transition-opacity">Melbourne Theranostics Innovation Center</span>
            </div>
          ))}
        </div>
      </div>
    </section>
  );
};

export default CollaboratorsMarquee;

/*
USAGE IN Index.tsx:
-------------------
1. Import at top: import CollaboratorsMarquee from '@/components/CollaboratorsMarquee';
2. Add between Careers and Connect sections:

      </section>  {/* end of Careers */}

      <CollaboratorsMarquee />

      {/* Connect */}
      <section id="connect" ...
*/
